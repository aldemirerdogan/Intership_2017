encoder
output = [ ]

if all pixels == 0
part = [size(1), size(2)]
output = cat ( output, part )
cat to the end

while all pixels ~= 0

if there is only one pixel
part = [ r,c, -8 ]
output = cat (output, part)

else
f8runner
part = vector, r, c, -8
output = cat (output, part)


decoder
input = output of encoder

counter = find number of the '-8'
use size and create ZEROS ( end - 1, end )

hold=1

for ( counter )

num = find '-8'

if num-4 - hold == 2
[r,c] = 1


else
use r and c to initialize ( num - 2, num - 1 )
run vector from 'hold' to 'num-4'
hold = num+1
